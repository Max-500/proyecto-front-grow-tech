// eslint-disable-next-line react/prop-types
const ButtonShared = ({ title }) => {
    return (
        <>
            <button className="button-form-shared" type="submit">{title}</button>
        </>
    )
}

export default ButtonShared;